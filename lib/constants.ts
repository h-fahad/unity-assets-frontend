// Dynamic data - categories are now fetched from backend via assets
// Sort options are still static as they represent UI functionality
export const SORT_OPTIONS = ['Most Downloaded', 'Latest', 'Price Low to High', 'Price High to Low'];
export const ROLES = ['guest', 'subscriber', 'admin']; 